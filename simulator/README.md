# Simulator

This directory will contain development simulators for field devices, GNSS
positions, machine telemetry, and sensor data.

Simulators should use the same versioned public contracts as physical devices.
No simulator implementation is present yet.
