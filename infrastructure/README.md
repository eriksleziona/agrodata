# Infrastructure

This directory will contain Dockerfiles, Docker Compose configuration, Nginx
configuration, deployment manifests, and operational runbooks.

The intended local stack includes the backend, frontend, PostgreSQL/PostGIS,
Redis, Mosquitto, MinIO, and Nginx. No infrastructure configuration or runnable
containers are included in this foundation.
