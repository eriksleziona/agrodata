# Tests

This directory is reserved for cross-module integration, contract, and
end-to-end tests. Unit tests should live close to the module they test when the
application structure is introduced.

The target test stack is Pytest for backend and edge components, Vitest for the
frontend, and Playwright for browser workflows. No tests are implemented yet.
