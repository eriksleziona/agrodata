# Edge

This directory is reserved for the Python edge agent that runs on supported
devices such as Raspberry Pi systems. It will coordinate GNSS/RTK, IMU, CAN,
LTE, sensor collection, local SQLite persistence, and synchronisation.

No device protocol or edge implementation is present yet.
