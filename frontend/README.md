# Frontend

This directory will contain the React and TypeScript web application built with
Vite. It will provide map-driven farm workflows using MapLibre, TanStack Query
for server state, and Zustand for focused client state.

No frontend application, dependencies, or build configuration is present yet.
