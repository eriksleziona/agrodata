# GIS

This directory is reserved for spatial-data documentation, processing tools, and
reference assets.

The platform will store spatial data in PostGIS and exchange geometry through
documented GeoJSON-based API contracts. No GIS implementation is present yet.
